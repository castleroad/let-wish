services:
  app:
    build: .
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
    restart: unless-stopped

  adminer:
    image: adminer
    ports:
      - "8080:8080"
    volumes:
      - .:/var/www/html
    restart: unless-stopped
